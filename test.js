// const child_process = require('child_process')
// const exec = child_process.exec;
// exec('git pull', { cwd: `` }, (error, stodut, stdeer) => {
//   console.dir(`stdout: ${stodut}`)
//   console.dir(`stdeer: ${stdeer}`)
// })
